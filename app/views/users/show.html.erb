<h1>
  <%= @user.username %>
</h1>

<dl>
  <dt>
    Private
  </dt>
    <dd>
      <%= @user.private %>
    
  <dt>
    Followers
  </dt>
    <dd>
      <%= @user.requests_recieved.where({ :status => 'accepted' }).count %>
    </dd>
    <dd>
      <% if @current_user != nil %>
        <% current_user_follow_status = @user.requests_recieved.where({ :sender_id => @current_user.id }).first %>
        <% if current_user_follow_status != nil %>
          <% if current_user_follow_status.status == 'accepted' %>
            <a href="/delete_follow_request/<%= current_user_follow_status.id %>">
              Unfollow
            </a>
          <% end %>
        <% end %>
      <% end %>
    </dd>

  <dt>
    Following
  </dt>
    <dd>
    <%= @user.requests_sent.where({ :status => 'accepted' }).count %>
</dl>


<ul>
  <li>
    <a href="/users/<%= @user.username %>">
      Profile
    </a>
  </li>

  
</ul>
